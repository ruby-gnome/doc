= class Gst::EventMask
Event mask.

== Object Hierarchy
* Object
  * GLib::Boxed
    * Gst::EventMask

== Instance Methods
--- type_flags
    This method returns all the flags available as an array of flags (see ((<GstEventFlag|Gst::Event#GstEventFlag>))).
    * Returns: all the flags available as an array of flags (see ((<GstEventFlag|Gst::Event#GstEventFlag>))).

--- type_id
    Gets the type id of the Gst::Event type referred by this
    event mask.
    * Returns: the type id (see ((<GstEventType|Gst::Event#GstEventType>))).

== See Also
((<Gst>)).

- ((<lrz>))
