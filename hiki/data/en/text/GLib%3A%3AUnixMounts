= module GLib::UnixMounts

== Module Functions

--- GLib::UnixMounts.changed_since?(time)

     Checks if the Unix mount have changed since a given Unix time.

     * Returns: true if the Unix mount have changed since ((|time|))

--- GLib::UnixMounts.get

     Gets an Array of an Array of GLib::UnixMount objects and a Unix mount
     timestamp.  The timestamp may be used to check for changes in mounts with
     GLib::UnixMount.changed_since?.

     * Returns: An Array of an Array of GLib::UnixMount objects and a Unix mount
       timestamp

== See Also

== ChangeLog


