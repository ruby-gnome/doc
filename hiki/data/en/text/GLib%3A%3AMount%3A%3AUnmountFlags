= class GLib::Mount::UnmountFlags

Flags used when an unmounting a mount. 

== Object Hierarchy

* Object
  * GLib::Flags
    * GLib::Mount::UnmountFlags

== Instance Methods

--- force?

     Checks if an unmount should be done even if there are outstanding file
     operations on the mount.

     * Returns: true if an unmount should be done even if there are outstanding
       file operations on the mount

--- none?

     Checks if any flags have been set.

     * Returns: true if no flags have been set

== Constants

--- UNMOUNT_FORCE

     Unmount even if there are outstanding file operations on the mount

--- UNMOUNT_NONE

     No flags set


== See Also

== ChangeLog


