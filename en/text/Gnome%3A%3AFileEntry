= class Gnome::FileEntry

== Object Hierarchy

* Object
  * GLib::Instantiatable
    * GLib::Object
      * GLib::InitiallyUnowned
        * Gtk::Object
          * Gtk::Widget
            * Gtk::Container
              * Gtk::Box
                * Gtk::VBox
                  * Gnome::FileEntry

== Included Modules

  * Gtk::Editable

== Class Methods

--- Gnome::FileEntry.new

     * Returns: self: ((*FIXME*))

== Instance Methods

--- browse_dialog_title

    Gets the Title for the Browse file dialog..
     * Returns: Title for the Browse file dialog.

--- browse_dialog_title=(browse_dialog_title)

    Sets the Title for the Browse file dialog..
     * browse_dialog_title: Title for the Browse file dialog.
     * Returns: browse_dialog_title

--- copy_clipboard

    See Gtk::Editable#copy_clipboard.

--- cut_clipboard

    See Gtk::Editable#cut_clipboard.

--- default_path

    Gets the Default path for the Browse file window..
     * Returns: Default path for the Browse file window.

--- default_path=(default_path)

    Sets the Default path for the Browse file window..
     * default_path: Default path for the Browse file window.
     * Returns: default_path

--- delete_selection

    See Gtk::Editable#delete_selection.

--- delete_text

    See Gtk::Editable#delete_text.

--- directory_entry=(directory_entry)

    Sets the value whether the file entry is being used to enter directory names or complete filenames. or not.
     * directory_entry: true if the file entry is being used to enter directory names or complete filenames.
     * Returns: directory_entry

--- directory_entry?

    Gets the value whether the file entry is being used to enter directory names or complete filenames. or not.
     * Returns: true if the file entry is being used to enter directory names or complete filenames.

--- editable=

    See Gtk::Editable#editable=.

--- editable?

    See Gtk::Editable#editable?.

--- filechooser_action

    Gets the type of operation that the file selector is performing..
     * Returns: The type of operation that the file selector is performing.

--- filechooser_action=(filechooser_action)

    Sets the type of operation that the file selector is performing..
     * filechooser_action: The type of operation that the file selector is performing.
     * Returns: filechooser_action

--- filename

    Gets the Filename that should be displayed in the file entry..
     * Returns: Filename that should be displayed in the file entry.

--- filename=(filename)

    Sets the Filename that should be displayed in the file entry..
     * filename: Filename that should be displayed in the file entry.
     * Returns: filename

--- get_chars

    See Gtk::Editable#get_chars.

--- get_full_path

     * Returns: self: ((*FIXME*))

--- gnome_entry

    Gets the Gnome::Entry that the file entry uses for entering filenames.  You can use this property to get the Gnome::Entry if you need to modify or query any of its parameters..
     * Returns: Gnome::Entry that the file entry uses for entering filenames.  You can use this property to get the Gnome::Entry if you need to modify or query any of its parameters.

--- gtk_entry

    Gets the Gtk::Entry that the file entry uses for entering filenames.  You can use this property to get the Gtk::Entry if you need to modify or query any of its parameters..
     * Returns: Gtk::Entry that the file entry uses for entering filenames.  You can use this property to get the Gtk::Entry if you need to modify or query any of its parameters.

--- history_id

    Gets the Unique identifier for the file entry.  This will be used to save the history list..
     * Returns: Unique identifier for the file entry.  This will be used to save the history list.

--- history_id=(history_id)

    Sets the Unique identifier for the file entry.  This will be used to save the history list..
     * history_id: Unique identifier for the file entry.  This will be used to save the history list.
     * Returns: history_id

--- insert_text

    See Gtk::Editable#insert_text.

--- modal=(modal)

    Sets the value whether the Browse file window should be modal. or not.
     * modal: true if the Browse file window should be modal.
     * Returns: modal

--- modal?

    Gets the value whether the Browse file window should be modal. or not.
     * Returns: true if the Browse file window should be modal.

--- paste_clipboard

    See Gtk::Editable#paste_clipboard.

--- position

    See Gtk::Editable#position.

--- position=

    See Gtk::Editable#position=.

--- ref_accessible

    See Atk::Implementor#ref_accessible.

--- select_region

    See Gtk::Editable#select_region.

--- selection_bounds

    See Gtk::Editable#selection_bounds.

--- set_browse_dialog_title(browse_dialog_title)

    Same as browse_dialog_title=.
     * browse_dialog_title: Title for the Browse file dialog.
     * Returns: self

--- set_default_path(default_path)

    Same as default_path=.
     * default_path: Default path for the Browse file window.
     * Returns: self

--- set_directory_entry(directory_entry)

    Same as directory_entry=.
     * directory_entry: true if the file entry is being used to enter directory names or complete filenames.
     * Returns: self

--- set_editable

    See Gtk::Editable#set_editable.

--- set_filechooser_action(filechooser_action)

    Same as filechooser_action=.
     * filechooser_action: The type of operation that the file selector is performing.
     * Returns: self

--- set_filename(filename)

    Same as filename=.
     * filename: Filename that should be displayed in the file entry.
     * Returns: self

--- set_history_id(history_id)

    Same as history_id=.
     * history_id: Unique identifier for the file entry.  This will be used to save the history list.
     * Returns: self

--- set_modal(modal)

    Same as modal=.
     * modal: true if the Browse file window should be modal.
     * Returns: self

--- set_position

    See Gtk::Editable#set_position.

--- set_title

     * Returns: self: ((*FIXME*))

--- set_use_filechooser(use_filechooser)

    Same as use_filechooser=.
     * use_filechooser: true if to use the new Gtk::FileChooser widget or the Gtk::FileSelection widget to select files.
     * Returns: self

--- title=

     * Returns: self: ((*FIXME*))

--- use_filechooser=(use_filechooser)

    Sets the value whether to use the new Gtk::FileChooser widget or the Gtk::FileSelection widget to select files. or not.
     * use_filechooser: true if to use the new Gtk::FileChooser widget or the Gtk::FileSelection widget to select files.
     * Returns: use_filechooser

--- use_filechooser?

    Gets the value whether to use the new Gtk::FileChooser widget or the Gtk::FileSelection widget to select files. or not.
     * Returns: true if to use the new Gtk::FileChooser widget or the Gtk::FileSelection widget to select files.

== Properties

--- browse-dialog-title: String (Read/Write)

    Title for the Browse file dialog.

--- default-path: String (Read/Write)

    Default path for the Browse file window.

--- directory-entry: true or false (Read/Write)

    Whether the file entry is being used to enter directory names or complete filenames.

--- filechooser-action: Gtk::FileChooserAction (Read/Write)

    The type of operation that the file selector is performing.

--- filename: String (Read/Write)

    Filename that should be displayed in the file entry.

--- gnome-entry: Gnome::Entry (Read)

    Gnome::Entry that the file entry uses for entering filenames.  You can use this property to get the Gnome::Entry if you need to modify or query any of its parameters.

--- gtk-entry: Gtk::Entry (Read)

    Gtk::Entry that the file entry uses for entering filenames.  You can use this property to get the Gtk::Entry if you need to modify or query any of its parameters.

--- history-id: String (Read/Write)

    Unique identifier for the file entry.  This will be used to save the history list.

--- modal: true or false (Read/Write)

    Whether the Browse file window should be modal.

--- use-filechooser: true or false (Read/Write)

    Whether to use the new Gtk::FileChooser widget or the Gtk::FileSelection widget to select files.

== Signals

--- activate: self

     * self: Gnome::FileEntry: ((*FIXME*))

--- browse-clicked: self

     * self: Gnome::FileEntry: ((*FIXME*))

== See Also

== ChangeLog


