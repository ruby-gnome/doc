= class Gnome::PrintJobPreview

== Object Hierarchy

* Object
  * GLib::Instantiatable
    * GLib::Object
      * GLib::InitiallyUnowned
        * Gtk::Object
          * Gtk::Widget
            * Gtk::Container
              * Gtk::Bin
                * Gtk::Window
                  * Gnome::PrintJobPreview

== Class Methods

--- Gnome::PrintJobPreview.new(job, title)

     
     * job: a Gnome::PrintJob object.
     * title: a title of the widget.
     * Returns: a Gnome::PrintJobPreview object.

--- nx

    Gets the Number of pages horizontally.
     * Returns: Number of pages horizontally

--- nx=(nx)

    Sets the Number of pages horizontally.
     * nx: Number of pages horizontally, allowed values are [1,65535]
     * Returns: nx

--- set_nx(nx)

    Same as Gnome::PrintJobPreview.nx=.
     * nx: Number of pages horizontally, allowed values are [1,65535]
     * Returns: self

--- ny

    Gets the Number of pages vertically.
     * Returns: Number of pages vertically

--- ny=(ny)

    Sets the Number of pages vertically.
     * ny: Number of pages vertically, allowed values are [1,65535]
     * Returns: ny

--- set_ny(ny)

    Same as Gnome::PrintJobPreview.ny=.
     * ny: Number of pages vertically, allowed values are [1,65535]
     * Returns: self

== Instance Methods

--- job

    Gets the Print job.
     * Returns: Print job

--- nx

    Gets the Number of pages horizontally.
     * Returns: Number of pages horizontally

--- nx=(nx)

    Sets the Number of pages horizontally.
     * nx: Number of pages horizontally
     * Returns: nx

--- ny

    Gets the Number of pages vertically.
     * Returns: Number of pages vertically

--- ny=(ny)

    Sets the Number of pages vertically.
     * ny: Number of pages vertically
     * Returns: ny

--- ref_accessible

    See Atk::Implementor#ref_accessible.

--- set_nx(nx)

    Same as nx=.
     * nx: Number of pages horizontally
     * Returns: self

--- set_ny(ny)

    Same as ny=.
     * ny: Number of pages vertically
     * Returns: self

== Properties

--- nx: Integer (Read/Write)

    Number of pages horizontally
    
    Allowed values: [1,65535]

--- ny: Integer (Read/Write)

    Number of pages vertically
    
    Allowed values: [1,65535]

--- job: Gnome::PrintJob (Read/Write)

    Print job

== See Also

== ChangeLog

* 2005-06-10 ((<kou>)): added

- ((<kou>))
