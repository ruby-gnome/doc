= class Poppler::ImageMapping

== Object Hierarchy

* Object
  * GLib::Boxed
    * Poppler::ImageMapping

== Instance Methods

--- area

     * Returns: self: ((*FIXME*))

--- area=

     * Returns: self: ((*FIXME*))

--- image

     * Returns: self: ((*FIXME*))

--- image=

     * Returns: self: ((*FIXME*))

--- set_area

     * Returns: self: ((*FIXME*))

--- set_image

     * Returns: self: ((*FIXME*))

== See Also

== ChangeLog


