= class Gnome::App

== Object Hierarchy

* Object
  * GLib::Instantiatable
    * GLib::Object
      * GLib::InitiallyUnowned
        * Gtk::Object
          * Gtk::Widget
            * Gtk::Container
              * Gtk::Bin
                * Gtk::Window
                  * Gnome::App

== Class Methods

--- Gnome::App.new

     * Returns: self: ((*FIXME*))

== Instance Methods

--- add_dock_item

     * Returns: self: ((*FIXME*))

--- add_docked

     * Returns: self: ((*FIXME*))

--- add_toolbar

     * Returns: self: ((*FIXME*))

--- app_id

    Gets the application ID string.
     * Returns: The application ID string

--- app_id=(app_id)

    Sets the application ID string.
     * app_id: The application ID string
     * Returns: app_id

--- contents=

     * Returns: self: ((*FIXME*))

--- create_menus

     * Returns: self: ((*FIXME*))

--- create_toolbar

     * Returns: self: ((*FIXME*))

--- dock

     * Returns: self: ((*FIXME*))

--- enable_layout_config

     * Returns: self: ((*FIXME*))

--- get_dock_item_by_name

     * Returns: self: ((*FIXME*))

--- insert_menus

     * Returns: self: ((*FIXME*))

--- install_menu_hints

     * Returns: self: ((*FIXME*))

--- menus=

     * Returns: self: ((*FIXME*))

--- name

     * Returns: self: ((*FIXME*))

--- prefix

     * Returns: self: ((*FIXME*))

--- ref_accessible

    See Atk::Implementor#ref_accessible.

--- remove_menu_range

     * Returns: self: ((*FIXME*))

--- remove_menus

     * Returns: self: ((*FIXME*))

--- set_app_id(app_id)

    Same as app_id=.
     * app_id: The application ID string
     * Returns: self

--- set_contents

     * Returns: self: ((*FIXME*))

--- set_menus

     * Returns: self: ((*FIXME*))

--- set_statusbar

     * Returns: self: ((*FIXME*))

--- set_statusbar_custom

     * Returns: self: ((*FIXME*))

--- set_toolbar

     * Returns: self: ((*FIXME*))

--- statusbar=

     * Returns: self: ((*FIXME*))

--- toolbar=

     * Returns: self: ((*FIXME*))

== Constants

--- CONFIGURABLE_ITEM_ABOUT
--- CONFIGURABLE_ITEM_CLEAR
--- CONFIGURABLE_ITEM_CLOSE
--- CONFIGURABLE_ITEM_CLOSE_WINDOW
--- CONFIGURABLE_ITEM_COPY
--- CONFIGURABLE_ITEM_CUT
--- CONFIGURABLE_ITEM_END_GAME
--- CONFIGURABLE_ITEM_FIND
--- CONFIGURABLE_ITEM_FIND_AGAIN
--- CONFIGURABLE_ITEM_HINT
--- CONFIGURABLE_ITEM_NEW
--- CONFIGURABLE_ITEM_NEW_GAME
--- CONFIGURABLE_ITEM_NEW_WINDOW
--- CONFIGURABLE_ITEM_OPEN
--- CONFIGURABLE_ITEM_PASTE
--- CONFIGURABLE_ITEM_PAUSE_GAME
--- CONFIGURABLE_ITEM_PREFERENCES
--- CONFIGURABLE_ITEM_PRINT
--- CONFIGURABLE_ITEM_PRINT_SETUP
--- CONFIGURABLE_ITEM_PROPERTIES
--- CONFIGURABLE_ITEM_QUIT
--- CONFIGURABLE_ITEM_REDO
--- CONFIGURABLE_ITEM_REDO_MOVE
--- CONFIGURABLE_ITEM_REPLACE
--- CONFIGURABLE_ITEM_RESTART_GAME
--- CONFIGURABLE_ITEM_REVERT
--- CONFIGURABLE_ITEM_SAVE
--- CONFIGURABLE_ITEM_SAVE_AS
--- CONFIGURABLE_ITEM_SCORES
--- CONFIGURABLE_ITEM_SELECT_ALL
--- CONFIGURABLE_ITEM_UNDO
--- CONFIGURABLE_ITEM_UNDO_MOVE
--- MENUBAR_NAME
--- PIXMAP_DATA
--- PIXMAP_FILENAME
--- PIXMAP_NONE
--- PIXMAP_STOCK
--- TOOLBAR_NAME
--- UI_BUILDER_DATA
--- UI_ENDOFINFO
--- UI_HELP
--- UI_INCLUDE
--- UI_ITEM
--- UI_ITEM_CONFIGURABLE
--- UI_RADIOITEMS
--- UI_SEPARATOR
--- UI_SUBTREE
--- UI_SUBTREE_STOCK
--- UI_TOGGLEITEM

== Properties

--- app-id: String (Read/Write)

    The application ID string

== See Also

== ChangeLog


